# AUTO-GENERATED FILE. DO NOT EDIT.
# Source: agent-team/sot/codex-runtime.manifest.toml

profile = "adlc_leader"
project_doc_fallback_filenames = ["AGENTS.md"]

[features]
multi_agent = true

[profiles.adlc_leader]
model = "gpt-5.3-codex"
model_reasoning_effort = "high"
model_verbosity = "medium"
approval_policy = "on-request"
sandbox_mode = "workspace-write"
model_instructions_file = "agents/adlc-leader.md"

[profiles.planner_pm]
model = "gpt-5.3-codex"
model_reasoning_effort = "medium"
model_verbosity = "medium"
approval_policy = "on-request"
sandbox_mode = "workspace-write"
model_instructions_file = "agents/planner-pm.md"

[profiles.builder]
model = "gpt-5.3-codex"
model_reasoning_effort = "medium"
model_verbosity = "medium"
approval_policy = "on-request"
sandbox_mode = "workspace-write"
model_instructions_file = "agents/builder.md"

[profiles.reviewer]
model = "gpt-5.3-codex"
model_reasoning_effort = "high"
model_verbosity = "medium"
approval_policy = "on-request"
sandbox_mode = "workspace-write"
model_instructions_file = "agents/reviewer.md"

[agents.adlc_leader]
description = "ADLC 리더 에이전트. 라우팅, 승인 게이트, 최종 의사결정 요약을 담당한다."
config_file = "agents/adlc-leader.toml"

[agents.planner_pm]
description = "기획/문제정의 에이전트. 가설과 검증 기준을 정의한다."
config_file = "agents/planner-pm.toml"

[agents.builder]
description = "실행 에이전트. 승인된 범위의 산출물을 생성한다."
config_file = "agents/builder.toml"

[agents.reviewer]
description = "검증 에이전트. 수용 기준 통과 여부와 리스크를 판단한다."
config_file = "agents/reviewer.toml"
